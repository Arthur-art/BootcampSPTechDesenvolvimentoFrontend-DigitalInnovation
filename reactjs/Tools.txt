-Tools


//instale o webpack no projeto 
- npm init -y
- npm i -D webpack webpack-cli

//crie um arquivo webpack.config.js e adicione o codigo abaixo

const path = require('path');

module.exports = {
    entry: './src/index.js',
    output: {
        path: path.resolve(__dirname, 'dist'),
        filename: 'bundle.js'
    }
};

//configure no json
- "start":"webpack --mode production"

//execute npm start - isso ira gerar uma pasta com o arquivo final o "bundle.js" que sera enviado


//instale o babel
npm i @babel/core babel-loader @babel/preset-env @babel/preset-react --save-dev

//Adicione os modules que serao usados pelo babel ao arquivo webpack

const path = require('path');
module.exports = {
    //onde o webpack vai pegar todos os arquivos pra montar toda a sua arvore
    entry: './index.js',
    output: {
        //dist - pasta onde estará arquivo final do projeto
        path: path.resolve(__dirname, 'dist'),
        filename: 'bundle.js'
    },
    module:{
        rules:[
            {
                test: /\.(js|jsx)$/,
                exclude:/node_modules/,
                use:{
                    loader: "babel-loader"
                }
            }
        ]
    }
};

//Instale o React DOM
- npm i react react-dom

//adicione no projeto um arquivo .babelrc
- {
    "presets":[
        "@babel/preset-env",
        "@babel/preset-react"
    ]
}